import ControlsSelect from './ControlsSelect';

export default {
  title: 'Features/Controls select',
  component: ControlsSelect,
  tags: ['autodocs'],
};

const Template = (args) => <ControlsSelect {...args} />;

export const Default = Template.bind({});
